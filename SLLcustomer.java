/*class AvlNode {
public int data;
public AvlNode left;
public AvlNode right;
public AvlNode parent;
public int height;

public AvlNode(int data) {
  this.data = data;
  this.left = null;
  this.right = null;
  this.parent = null;
  this.height = 0;
}
}*/

public class SLLcustomer extends SLL//ConstructAVLTree  

{
 
     //private video rootNode; //have video extend node and use video instead 
     //private video leftVideo = super.leftChild;
     //private video rightVideo = super.rightChild;
     //boolean inStore = false;

     

      
     //public VideoStore()
     //{
         
      //rootNode = null;  
       
     //}
     
     
     
     
     
     //public class VideoStore extends AVLTreeST//ConstructAVLTree  

//{
 
     //private video rootNode; //have video extend node and use video instead 
     //private video leftVideo = super.leftChild;
     //private video rightVideo = super.rightChild;
     //boolean inStore = false;



      
     //public VideoStore()
     //{
         
      //rootNode = null;  
       
     //}

/*name: String
− id: String*/
//− rentVideo: Data Structure<… Video>

//Video rentVideo = new Video();
     
     
//+ Customer(…)
/*+ getName(): String
+ getId(): String
+ getRentVideo(): Data Structure<… Video>
+ setRentVideo(…): void*/

            
     
     
             //− setVideoInStore(…): void
            //public SLLvideo setRentVideo()
            //{
            
            //this.head = head;
           
            //Node root = null;
           
            //videoInStore = videoInStore.insert(root, video);
              //root.data = input;
              
              //put(rentInput,rentInput);                       
              //inStore = true;


             //}
             
             //− checkOut(…): boolean
             
             public SLLvideo getRentVideo()
             {
               /*SLLvideo temp = this.head.rentVideo; 
               
               while(!temp.head.getElement().equals(target))
                 temp.head = temp.head.getNext();
               //return element;
               return temp;*/
               return this.head.rentVideo;
             }  

        
                 
               
            //public      
                 
                 
                 
                 
                 
               //public void getRentVideo()  //: Data Structure<… Video>
               //{
                  //ownVid.getTitles()

                 /*String buffer = rootNode.element;
                 
                 while (rootNode.element == target)
                 {
                 if (buffer.compareTo(target) < 0)  
                    rootNode = rootNode.leftChild;  
                 else if (buffer.compareTo(target) > 0)
                    rootNode = rootNode.rightChild;  
                 }    
                 rootNode = null;*/
                 //inStore = false;
                 //return inStore;
                 
               //}
             
             /*public boolean ccheckIn(String target)
             {
                 rentVideo.delete(target);

                 //setRentInStoreInStore(target);
                 /*String buffer = rootNode.element;
                 
                 while (rootNode.element == target)
                 {
                 if (buffer.compareTo(target) < 0)  
                    rootNode = rootNode.leftChild;  
                 else if (buffer.compareTo(target) > 0)
                    rootNode = rootNode.rightChild;  
                 }    
                 rootNode = null;/
                 //inStore = true;
                 //return inStore;
                 
             }*/

             
             //public boolean inStore()

             //− printAllVideos(): void
             //public void getRentVideo()
             //{
                 
               //this.inorder();  
 
             //}     
             

            //VideoStore videoInStore = new VideoStore();

//}


     
     
     
     
     
     
     
     
     
            /* //− setVideoInStore(…): void
            public void setVideoInStore(String input)
            {
            //this.head = head;
           
            //Node root = null;
           
            //videoInStore = videoInStore.insert(root, video);
              //root.data = input;
              put(input,input);                       
              inStore = true;


             }
             
             //− checkOut(…): boolean
             public boolean checkOut(String target)
             {
                 delete(target);
                 /*String buffer = rootNode.element;
                 
                 while (rootNode.element == target)
                 {
                 if (buffer.compareTo(target) < 0)  
                    rootNode = rootNode.leftChild;  
                 else if (buffer.compareTo(target) > 0)
                    rootNode = rootNode.rightChild;  
                 }    
                 rootNode = null;
                 inStore = false;
                 return inStore;
                 
             }
             
             public boolean checkIn(String target)
             {
                 setVideoInStore(target);
                 /*String buffer = rootNode.element;
                 
                 while (rootNode.element == target)
                 {
                 if (buffer.compareTo(target) < 0)  
                    rootNode = rootNode.leftChild;  
                 else if (buffer.compareTo(target) > 0)
                    rootNode = rootNode.rightChild;  
                 }    
                 rootNode = null;
                 inStore = true;
                 return inStore;
                 
             }

             
             //public boolean inStore()

             //− printAllVideos(): void
             public void printAllVideos()
             {
                 
               this.inorder();  
 
             }     
             

            //VideoStore videoInStore = new VideoStore();*/

}

